# 持久化資料儲存 (Persistent Storage)：讓成品擁有「記憶力」

> 深入解析如何在不需要後端資料庫與伺服器的情況下，利用瀏覽器本地儲存（`localStorage`）讓 Artifact 成品記住使用者的歷史紀錄與資料。

---

## 💡 痛點：為什麼你的網頁小工具需要持久化？

如果沒有持久化設定，一般的網頁小工具（如待辦事項清單、記帳表、表單）：
- 只要使用者**按 F5 重新整理**、**切換瀏覽器分頁**或**關閉網頁**；
- 剛剛輸入的 10 筆資料、勾選狀態或計算結果就會**瞬間蒸發歸零**！

**持久化資料儲存（Persistent Storage）** 就是**替網頁加上記憶體**，讓使用者今天輸入的資料，明天再次打開網頁依然完好如初。

---

## 🛠️ 最實用的實作技術：瀏覽器本地儲存 (`localStorage`)

### 1. 運作原理
- **零成本、免伺服器**：資料直接儲存在使用者當前使用的瀏覽器（Chrome / Safari / Edge）或手機沙盒中。
- **無跨站風險**：不需要架設 MySQL、PostgreSQL 等複雜資料庫，程式碼直接內嵌在單一 HTML/React Artifact 內。

### 2. 核心 Prompt 咒語（直接複製給 Claude）
當你請 Claude 設計可輸入資料的工具時，請加上這段約束：

```text
請使用瀏覽器內建的 localStorage 來保存使用者輸入的所有資料與勾選狀態。
確保使用者在重新整理頁面（F5）或重開瀏覽器後，所有紀錄依然完整保留。
並請在介面上提供一個具備二次確認彈窗的「清空全部資料」重置按鈕。
```

---

## 🏢 職場實戰三大情境

1. **活動籌備檢核清單（Checklist）**：
   同仁陸續勾選「場地確認」、「簡報印製完成」，下次開會點開連結時進度清清楚楚，不用重勾。
2. **部門零用金與採購記帳本**：
   每天登記採購文具明細，系統自動加總花費，歷史流水號自動保存。
3. **個人化偏好記憶**：
   記住使用者自訂的字體大小、暗黑模式（Dark Mode）開關狀態。

---

## ⚠️ 邊界與限制

| 特性 | 說明 | 建議處置 |
| :--- | :--- | :--- |
| **資料跟隨單一裝置** | 在辦公室電腦輸入的資料，用手機點開分享連結不會同步（兩端各自獨立）。 | 若需要跨團隊跨裝置即時共同編輯，建議串接 Google Sheets 或正式後端。 |
| **清除快取會清空** | 若使用者點擊「清除瀏覽器歷史紀錄/快取」或使用「無痕模式」，儲存的資料會被重置。 | 在介面上設計「匯出 JSON / CSV」功能供使用者備份。 |

---

← [返回 Artifacts 主手冊](../README.md)
